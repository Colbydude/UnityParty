using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace UnityParty
{
    public class BoardSpaceGameObject : MonoBehaviour
    {
        // =====================================================
        // Publics
        public BoardSpaceGameObject NextSpace;
        public BoardSpaceGameObject PreviousSpace;
        public SpaceFlags Flags;

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
